server:
  port: 31000

spring:
  application:
    name: ribbon-consumer
  cloud:
    loadbalancer:
      enabled: false

eureka:
  client:
    # 服务注册中心地址
    service-url:
      defaultZone: http://localhost:20000/eureka


